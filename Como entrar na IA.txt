Como entrar na IA:

Copiar o caminho da pasta (Estudo caso 1)

aplicar "cd + caminho da pasta

Colocar:

conda create --name dsaec1 python=3.13

# Ativar o ambiente:

conda activate dsaec1

# Instalar o pip e as dependências:

conda install pip

pip install -r requirements.txt 

Executar a app:

streamlit run pymentor.py